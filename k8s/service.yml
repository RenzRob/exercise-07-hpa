apiVersion: v1
kind: Service
metadata:
  name: node-registry
  labels:
    app: node-registry
spec:
  selector:
    app: node-registry
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP